### Solution 1
`solution.js` uses the convert to string method in order to facilitate working with comparison
not the optimal in term of speed due to strings overhead

### Solution 2
`solution2.js` as suggested by the leetcode exercise i was encouraged to try resolving it wihtout
using/converting to strings and the result is faster working directly with numbers